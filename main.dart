//main file contains the main()

//The main method
void main() {
  //DART OPERATORS

  //Arithmetic Operators

  int num1 = 10;
  int num2 = 20;
  // print(num1 + num2);
  // print(num1 - num2);
  // print(num1 * num2);
  // print(num1 / num2);
  // print(num1 % num2);
  //print(num1 ~/ num2);
  // print(num1++);
  // num1 += 2;

  // print(num1);
  // print(num1--);
  // print(++num1);
  // print(--num1);
  // List userData = [];
  //  if(userData.length == 0) {
  //     print('No data');
  //   } else {
  //     print('Data available');
  //  }
  //Relational Operators
  print(num1 > num2); //T
  print(num1 < num2); //T
  print(num1 >= num2); //F
  print(num1 <= num2); //T
  print(num1 == num2); //F
  print(num1 != num2); //T

  //Logical operators
  // print(num1 > num2 && num1 < num2);
  // print(num1 > num2 || num1 < num2);
  // print(!(num1 > num2 && num1 < num2));
  // print(!(num1 > num2 || num1 < num2));
  // print(!(num1 > num2 && num1 < num2) && (num1 > num2 || num1 < num2));
  // print(!(num1 > num2 && num1 < num2) || (num1 > num2 || num1 < num2));
  // print(!(num1 > num2 && num1 < num2) && (num1 > num2 || num1 < num2) || (num1 > num2 || num1 < num2));
  // print(!(num1 > num2 && num1 < num2) && (num1 > num2 || num1 < num2) && (num1 > num2 || num1 < num2));
  // print(!(num1 > num2 && num1 < num2) || (num1 > num2 || num1 < num2) || (num1 > num2 || num1 < num2));
  // print(!(num1 > num2 && num1 < num2) && (num1 > num2 || num1 < num2) || (num1 > num2 || num1 < num2) || (num1 > num2 || num1 < num2));
  // print(!(num1 > num2 && num1 < num2) && (num1 > num2 || num1 < num2) && (num1 > num2 || num1 < num2) || (num1 > num2 || num1 < num2));
  // print(!(num1 > num2 && num1 < num2) && (num1 > num2 || num1 < num2) || (num1 > num2 || num1 < num2) || (num1 > num2 || num1 < num2) || (num1 > num2 || num1 < num2));
  // print(!(num1 > num2 && num1 < num2) && (num1 > num2 || num1 < num2) && (num1 > num2 || num1 < num2) || (num1 > num2 || num1 < num2) || (num1 > num2 || num1 < num2));
  // print(!(num1 > num2 && num1 < num2) && (num1 > num2 || num1 < num2) && (num1 > num2 || num1 < num2) && (num1 > num2 || num1 < num2) || (num1 > num2 || num1 < num2));
  // print(!(num1 > num2 && num1 < num2) && (num1 > num2 || num1 < num2) && (num1 > num2 || num1 < num2) && (num1 > num2 || num1 < num2) || (num1 > num2 || num1 < num2) || (num1 > num2 || num1 < num2));
}
